# HTTP port for the backend.
# On Render this is provided by the platform; locally you can override.
PORT=4000

# Comma-separated list of allowed origins for CORS.
# By default only the production frontend is allowed.
ALLOWED_ORIGINS=https://privacybackroom.app

# Log level placeholder (not strictly used yet, but ready for extension)
LOG_LEVEL=info
